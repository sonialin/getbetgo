<div class="row">
	<div class="col-lg-8">

	  <!-- the actual blog post: title/author/date/content -->
	  <h1><%= @post.title %></h1>
	  <p class="lead">by <%= link_to @post.user.name, user_path(@post.user.id) %> </p>
	  <% if !@post.paid? %>
		  <%= link_to 'Pay now', {:controller => :posts, :action=> :payment, :id => @post}, :class => 'btn btn-primary' %>
	  <% end %>
	  <% if current_user == @post.user %>
	  	<br />
			<%= link_to 'Edit', edit_post_path(@post) %>
		<% end %>
	  <hr>
	  <p><span class="glyphicon glyphicon-time"></span>&nbsp;Created at<%= @post.created_at %>&nbsp;by&nbsp;<%= @post.user.name %></p>
	  <hr>
	  <%= image_tag @post.image_url, :class => "img-responsive" %>
	  <%= image_tag @post.image.url(:big) %>
	  <hr>
	  <p class="lead"><%= @post.description %></p>
	  <p><strong>Placeholder text by:</strong></p>
	  <ul>
	    <li><a href="http://spaceipsum.com/">Space Ipsum</a></li>
	    <li><a href="http://cupcakeipsum.com/">Cupcake Ipsum</a></li>
	    <li><a href="http://tunaipsum.com/">Tuna Ipsum</a></li>
	  </ul>
	  
	  <hr>
	  <!-- Enable current user to claim a bet -->
	  <div class="well">
	    <% if user_signed_in? %>
	      <h4>Submit Your Bet:</h4>
	      	<%= form_for([@post, @bet]) do |f| %>
	          <div class="form-group">
	            <%= f.text_area :body, :class => 'form-control', :rows => '3' %><br />
	            <%= f.submit :class => "btn btn-primary" %>
	          </div>
	        <% end %>
	    <% else %>
	      <div>
	        <p>Please log in to create a bet</p>
	      </div>
    	<% end %>
    <!-- End bet box -->
    </div>

	  <!-- the bets -->
	  <% @bets.each do |bet| %>
	  	<h3><%= bet.user.name %><small><%= bet.created_at %></small></h3>
	  	<p><%= bet.body %></p>
	  	<% if bet.open? %>
	  		<% if @post.user == current_user %>
	  			<%= button_to select_post_bet_path(@post, bet), :method => :post do %>
          	Select
          <% end %>
	  		<% end %>
	  	<% elsif bet.selected? %>
	  		<p>selected</p>
  			<% if bet.user == current_user %>
		  		<h4>Submit Your Update:</h4>
		    	<%= form_for([@post, bet, @update], :url => post_bet_updates_path(@post, bet)) do |f| %>
		        <div class="form-group">
		          <%= f.text_area :body, :class => 'form-control', :rows => '3' %><br />
		          <%= f.submit :class => "btn btn-primary" %>
		        </div>
		      <% end %>
  			<% end %>
	  	<% elsif bet.submitted? %>
	  		<p>submitted</p>
	  		<% if bet.update %>
	  			<%= bet.update.body %>
	  		<% end %>
	  	<% end %>
	  <% end %>
	</div>

	<div class="col-lg-4">
	  <div class="well">
	    <h4>Blog Search</h4>
	    <div class="input-group">
	      <input type="text" class="form-control">
	      <span class="input-group-btn">
	        <button class="btn btn-default" type="button"><span class="glyphicon glyphicon-search"></span></button>
	      </span>
	    </div><!-- /input-group -->
	  </div><!-- /well -->
	  <div class="well">
	    <h4>Popular Blog Categories</h4>
	      <div class="row">
	        <div class="col-lg-6">
	          <ul class="list-unstyled">
	            <li><a href="#dinosaurs">Dinosaurs</a></li>
	            <li><a href="#spaceships">Spaceships</a></li>
	            <li><a href="#fried-foods">Fried Foods</a></li>
	            <li><a href="#wild-animals">Wild Animals</a></li>
	          </ul>
	        </div>
	        <div class="col-lg-6">
	          <ul class="list-unstyled">
	            <li><a href="#alien-abductions">Alien Abductions</a></li>
	            <li><a href="#business-casual">Business Casual</a></li>
	            <li><a href="#robots">Robots</a></li>
	            <li><a href="#fireworks">Fireworks</a></li>
	          </ul>
	        </div>
	      </div>
	  </div><!-- /well -->
	  <div class="well">
	    <h4>Side Widget Well</h4>
	    <p>Price: <%= @post.price %></p>
	    <p>Quantity: <%= @post.quantity %></p>
	    <p>Total fund: <%= @post.total %></p>
	    <p>Total bets submitted: <%= @post.bets.size %></p>
	    
	    <p>Betters:</p>
	    <% if @post.bets.any? %>
		    <% @post.bets.each do |bet| %>
		    	<p><%= bet.user.name %></p>
		    <% end %>
		  <% else %>
		  	<p>There is no bet submitted yet.</p>
	    <% end %>
	  </div><!-- /well -->

	</div>
</div>






