$('#my-posts').append('<%= j render @posts %>');
$('.pagination').replaceWith('<%= j will_paginate @posts, renderer: BootstrapPagination::Rails %>');
<% if @posts.next_page %>
	$('#with-button .pagination').hide();
<% else %>
	$('#with-button .pagination, #load_more_posts').remove();
<% end %>