<%= link_to post_bet_path(activity.trackable.bet.post, activity.trackable.bet) do %>
	<%= activity.owner.name if activity.owner %>
	submitted an update on
	<%= activity.trackable.post.title %>
<% end %>